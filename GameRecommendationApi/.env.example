# Ova datoteka sadrži sve varijable koje aplikacija i Docker koriste.

### Neo4j (backend - driver) ###
# Koristi se u `appsettings.json` ili iz env var-a
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=password123

# Docker: format koji Neo4j kontejner očekuje (username/password)
# Ako koristite Docker Compose, ovo će biti proslijeđeno kontejneru
NEO4J_AUTH=neo4j/password123

### IConfiguration ###
# ASP.NET Core može čitati i ove varijable (equivalent njima iznad)
Neo4j__Uri=bolt://localhost:7687
Neo4j__Username=neo4j
Neo4j__Password=password123

### Authorization / API key ###
# Backend očekuje `Authorization:ApiKey` (Middleware čita Authorization:ApiKey)
AUTHORIZATION_APIKEY=replace_with_secure_key
# alternativni IConfiguration format
Authorization__ApiKey=replace_with_secure_key

### Logging (override) ###
# Ispravan format za ASP.NET Core (dupla donja crta -> section separator)
Logging__LogLevel__Default=Information
# segment sa tačkom zadržava tačku u imenu sekcije
Logging__LogLevel__Microsoft.AspNetCore=Warning


